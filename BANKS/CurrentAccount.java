
package BANKS;
class CurrentAccount extends BankAccount {
    public CurrentAccount(String accountHolderName, int accountNumber, double initialBalance, int pin) {
        super(accountHolderName, accountNumber, initialBalance, pin);
    }
}
